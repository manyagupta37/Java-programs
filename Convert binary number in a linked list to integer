class Solution {
    public int getDecimalValue(ListNode head) {
        int decimal =0;
        ListNode temp=head;
        while(temp!=null)
        {
            decimal= decimal*2 + temp.val;
            temp=temp.next;
        }
        return decimal;
    }
}
